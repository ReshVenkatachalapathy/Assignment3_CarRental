using System;

public class Car : Vehicle
{
	public Car(string manufacturer, string model, int year, int rentalPrices, 
		int seats, string engineType, string transmission) : base(model, manufacturer, year, rentalPrices)

	{

	}
}
